---
title: "nwt_pikas_vignette"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{nwt_pikas_vignette}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(alohaal)
library(tidyverse)

ggplot(data = nwt_pikas %>% filter(!is.na(sex)), aes(x = rectal_temp, y = weight)) + geom_point(aes(color = sex, shape = sex), alpha = 0.8) +
  theme_minimal() +
  labs(title = "Rectal Temperatures and Weights of Male and Female Pikas",
       y = "Weight", x = "Rectal Temperature")

ggplot(data = nwt_pikas %>% filter(!is.na(sex)), aes(x = sex, y = weight)) + geom_boxplot(aes(color = sex, shape = sex), alpha = 0.8, width = 0.5) +
  theme_minimal() +
  labs(title = "Weights of Male and Female Pikas",
       y = "Weight")

ggplot(data = nwt_pikas, aes(x = local_site, y = weight)) + geom_boxplot(aes(color = local_site, shape = local_site), alpha = 0.8, width = 0.5) +
     theme_minimal() +
     labs(title = "Weights of Male and Female Pikas",
          y = "Weight")

ggplot(data = nwt_pikas %>% filter(!is.na(stage)), aes(x = stage, y = weight)) + geom_boxplot(aes(color = stage, shape = stage), alpha = 0.8, width = 0.5) +
     theme_minimal() +
     labs(title = "Weights of Male and Female Pikas",
          y = "Weight")
```
